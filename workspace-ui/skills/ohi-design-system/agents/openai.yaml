interface:
  display_name: "ohi Design System"
  short_description: "Build new features with the ohi workspace system"
  default_prompt: "Use $ohi-design-system to build this feature with the established workspace components, themes, and interaction rules."
policy:
  allow_implicit_invocation: true
