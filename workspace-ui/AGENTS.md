# ohi UI implementation contract

Read and apply [ohi-design-system](skills/ohi-design-system/SKILL.md) for every new feature or visual change in this project. This applies automatically; the user need not name the skill each time.

The reusable source is `src/`, and `app/` is its live worked example. Read actual exports and theme tokens before editing. Extend shared components rather than introducing parallel styles or controls. Follow current user decisions and the skill's validation gate. Preserve this instruction file and `skills/` if moving this standalone project to another repository.
