NEXT_PUBLIC_SUPABASE_URL=https://dozeanhsgkjztmewtlqd.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_olqPKPERqjkLBt7yIMaIvw_Nha99a8V
# Server-only Supabase admin key. Never prefix this with NEXT_PUBLIC_.
SUPABASE_SECRET_KEY=
DATABASE_URL=
OPENAI_API_KEY=
# Marketing Content Studio image generation (optional). Defaults to gpt-image-1.
# The studio uses OPENAI_API_KEY for news discovery, drafting and image generation;
# without it, those features fall back to local starter content. Media uploads use
# the Supabase Storage bucket "content-media" created by the studio migration.
OPENAI_IMAGE_MODEL=

# Admin workspace
NEXT_PUBLIC_SITE_URL=http://localhost:3000
ADMIN_ALLOWLIST=soul@physical-io.com

# Legacy libSQL settings retained for old local workflows. The admin UI uses Supabase.
TURSO_DATABASE_URL=
TURSO_AUTH_TOKEN=

# Email sending (optional). Campaigns still track locally without this.
RESEND_API_KEY=
RESEND_FROM=Physical I/O <updates@physical-io.com>
RESEND_WEBHOOK_SECRET=

# From address for the "Ask Us Anything" form (/askusanything). Uses RESEND_API_KEY
# above. The domain must be verified in Resend.
# Defaults to "Physical I/O <ask@physical-io.com>" if unset.
ASK_FROM_EMAIL=
